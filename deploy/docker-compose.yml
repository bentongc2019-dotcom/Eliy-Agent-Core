version: '3.8'
services:
  eliy-webchat:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - ../.env
    restart: unless-stopped
